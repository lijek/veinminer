# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
# Disables gradle's buggy (in minecraft's case) daemon.
# Also makes builds more reliable and less likely to randomly fail.
org.gradle.daemon=false

# Fabric Properties
	# check these on https://fabricmc.net/use
	minecraft_version=b1.7.3
	yarn_mappings=47ea3ba
	loader_version=1.0.0
    stapi_version=45f969c

	gcapi_version=0e6225f
	hmi_version=f72ba3a
	modmenu_version=v1.8.5-beta.3

# Mod Properties
# http://maven.apache.org/guides/mini/guide-naming-conventions.html
	mod_version = 1.0.0
	maven_group = pl.lijek
	archives_base_name = Veinminer
